//- .wrapper.ohnooooo
//-   - const upCat = cat.toUpperCase();
//-   // divs are assumed, so the above is equivalent to"div.wrapper.ohnooooo"
//-   p.hello Hello, my cat's name is #{upCat}!
//-   p.hello Hello, my cat's name is #{cat.toUpperCase()}! 
//-   //- it's #{} in order to interpolate vars within text
//-   span#yo Yo!!
//-   img.cat(src="cat.jpg" alt=`Cat ${cat}`)
//-     //- it's `${}` within ticks (`) (object literals) in order to interpolate vars within attributes
//- 
//- h2 
//-   | Hello
//-   em How are you?
//-   

extends layout

//- block header
//-   h2 Yo!

//- block content
  //- h2 Sale ends in #{h.moment().endOf('day').fromNow()}
