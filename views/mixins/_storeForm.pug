mixin storeForm(store = {})
  form(action=`/add/${store._id || ''}` method="POST" class="card")
    //- The action=`/add/${store._id || ''}` is a nice trick we can use in order to reuse an existing form for both "adding" and "updating"
    label(for="name") Name
    input(type="text" name="name" value=store.name)
    label(for="description") Description
    textarea(name="description")= store.description
    //- address, lng and lat...
    label(for="address")
    input(type="text" id="address" name="location[address]")
    //- The weird "location[address]" syntax signifies the address property of the location object in the storeSchema inside Store.js.  This syntax is made possible by the body parser module required in App.js (app.use(bodyParser.urlencoded({ extended: true }));)
    - const choices = ['Wifi', 'Open Late', 'Family Friendly', 'Vegetarian', 'Licensed']
    - const tags = store.tags || []
    ul.tags
      each choice in choices 
        .tag.tag__choice
          input(type="checkbox" id=choice value=choice name="tags" checked=(tags.includes(choice)))
          label(for=choice) #{choice}
          //- label(for=choice)= choice
    input(type="submit" value="Save -->" class="button")
